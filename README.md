# 📦 Rust Warehouse Management System (WMS)

A robust Command-Line Interface (CLI) application built with **Rust** for efficient inventory management.

## 🚀 Features
- **Full CRUD:** Add, List, Search, and Delete products.
- **Persistent Storage:** Automatically saves and loads data from a `CSV` file.
- **Smart UI:** Perfectly aligned terminal tables for clear data visualization.
- **Robustness:** Handles invalid inputs and ensures data integrity.

## 🛠️ Tech Stack
- **Language:** Rust 🦀
- **Serialization:** Serde & Serde_JSON
- **Storage:** CSV Crate

## 🏁 How to Run
1. Ensure you have Rust installed (`cargo --version`).
2. Clone the repo: `git clone https://github.com/koktel141/rust-inventory-manager`
3. Run the app: `cargo run`

---
*Developed as part of my journey into System-level programming and AI-integrated systems.*